server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eventdb
    username: postgres
    password: postgres
    jpa:
      hibernate:
      ddl-auto: none   # we already created tables in pgAdmin
      show-sql: true